# Problem

Average Population of Each Continent

# Solution

SELECT COUNTRY.CONTINENT,FLOOR(SUM(CITY.POPULATION)/COUNT(CITY.POPULATION))
FROM CITY
JOIN COUNTRY
ON CITY.COUNTRYCODE=COUNTRY.CODE GROUP BY CONTINENT